// sections
import { BuilderView } from 'src/sections/builder/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'پیشخوان - مدیریت صفحات',
};

export default function BuilderPage() {
  return <BuilderView />;
}
