// sections
import { FileManagerView } from 'src/sections/file-manager/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'پیشخوان - مدیریت فایل',
};

export default function FileManagerPage() {
  return <FileManagerView />;
}
