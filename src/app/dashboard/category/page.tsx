// sections
import { CategoryListView } from 'src/sections/category/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'پیشخوان - مدیریت دسته بندی ها',
};

export default function CategoryListPage() {
  return <CategoryListView />;
}
