// sections
import { AccessListView } from 'src/sections/access/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'پیشخوان - مدیریت سطح دسترسی',
};

export default function AccessListPage() {
  return <AccessListView />;
}
